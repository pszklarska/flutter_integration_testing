name: flutter_integration_test
description: Example of Flutter Integration Testing on Firebase Test Lab
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test: ^1.0.2+2

flutter:
  uses-material-design: true